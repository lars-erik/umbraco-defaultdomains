### Automatic UrlRewriteNet redirects for canonical domains

Adds "Default Domains" datatype.
Uses first value of that type for domain root nodes
to create automatic UrlRewriteNet permanent redirects.